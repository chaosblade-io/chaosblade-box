chaos:
  agent:
    helm: ${CHAOS_AGENT_HELM_URL:https://chaosblade.oss-cn-hangzhou.aliyuncs.com/platform/release/${chaos.agent.version}/chaosblade-box-agent-${chaos.agent.version}.tgz}
    repository: ${CHAOS_AGENT_REPOSITORY:chaosbladeio/chaosblade-agent}
    url: ${CHAOS_AGENT_URL:https://chaosblade.oss-cn-hangzhou.aliyuncs.com/platform/release/${chaos.agent.version}/chaosagent.tar.gz}
    version: ${CHAOS_AGENT_VERSION:1.0.2}
  cache:
    auto-sync: true
    distributed-cache:
      expire-after-access: 900
      expire-after-write: 900
      name: redis
    enable: true
    local-cache:
      expire-after-access: 900
      expire-after-write: 900
      max-size: 100
      name: heap
    spring:
      enable: true
  env: daily
  function:
    sync:
      type: ALL
  server:
    domain: ${CHAOS_SERVER_DOMAIN:}
  sync:
    chaosblade:
      on-startup: true
  prometheus:
    api: ${PROMETHEUS_API_URL:}
    job: xhas
cloud:
  agent:
    version: '"1.16.3"'
management:
  port: ${MANAGEMENT_PORT:7002}
module:
  scope: Chaos
mybatis:
  mapperLocations: classpath*:mapper/*.xml
mybatis-plus:
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mybatis/*.xml
  type-enums-package: com.alibaba.chaosblade.box.common.common.enums
  type-handlers-package: com.alibaba.chaosblade.box.dao.mapper.type
product:
  type: public
regionId:
  default: '"cn-hangzhou"'
scheduler:
  engine: quartz
security:
  basic:
    enabled: false
server:
  port: ${SERVER_PORT:7001}

logging:
  level:
    root: info
    org.springframework: info
    com.alibaba.chaosblade.box: info

spring:
  aop:
    proxy-target-class: true
  data:
    cassandra:
      cluster-name: ''
  datasource:
    continue-on-error: true
    data: classpath:sql/data/*.sql
    # MySQL 8 使用新的驱动类
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
    initialize: true
    password: ${DB_PASSWORD:Zmzzmz010627!}
    schema: classpath:sql/schema/chaosblade-box-ddl.sql
    type: com.zaxxer.hikari.HikariDataSource
    # MySQL 8 连接配置
    url: ${DB_URL:jdbc:mysql://localhost:3306/chaosblade?characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10}
    username: ${DB_USERNAME:root}
    # HikariCP 连接池配置
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 60000
      idle-timeout: 600000
      max-lifetime: 1800000
      # 增加锁等待超时时间
      connection-init-sql: SET SESSION innodb_lock_wait_timeout = 120
      # MySQL 8 连接测试查询
      connection-test-query: SELECT 1
  mcms:
    enabled: false
  mvc:
    throw-exception-if-no-handler-found: true
  thymeleaf:
    mode: HTML5
    prefix: classpath:/build
  velocity:
    enabled: false

# 压测引擎配置
chaosblade:
  loadtest:
    engine:
      host: ${LOADTEST_ENGINE_HOST:localhost:8008}


# 探测任务服务基地址（可覆盖），默认为文档示例地址
probe:
  api:
    base-url: ${PROBE_API_BASE_URL:http://1.94.151.57:8101}
  topo:
    base-url: ${PROBE_TOPO_BASE_URL:http://1.94.151.57:8106}
